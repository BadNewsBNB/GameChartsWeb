# ✅ Cloudflare Workers 部署检查清单

## 📁 文件检查

确保以下文件存在并已提交到 Git：

- [x] `wrangler.toml` - Workers 配置文件
- [x] `workers-site/index.js` - Worker 入口文件
- [x] `workers-site/package.json` - Workers 依赖
- [x] `package.json` - 包含 `@cloudflare/kv-asset-handler`
- [x] `dist/` - 构建输出（会在 CI/CD 中自动生成）

## 🔧 Cloudflare 控制台设置

### 当前配置（从你的截图）：
```
✅ 名称: gameschartweb
✅ Git 存储库: BadNewsBNB/GameChartsWeb
✅ 构建命令: npm run build
✅ 部署命令: npx wrangler deploy
✅ 生产分支: main
```

### 需要检查的配置：
- [ ] **根目录**: 
  - 如果 `GameChartsWeb` 在仓库根目录 → 设置为 `/`
  - 如果 `GameChartsWeb` 在子目录 → 设置为 `/GameChartsWeb`

## 🚀 部署步骤

### 步骤 1: 提交代码
```bash
cd GameChartsWeb
git add .
git commit -m "Add Cloudflare Workers configuration"
git push origin main
```

### 步骤 2: 等待自动部署
- 登录 https://dash.cloudflare.com/
- 进入 Workers & Pages → gameschartweb
- 查看 Deployments 标签
- 等待构建完成

### 步骤 3: 验证部署
- 构建成功后会显示部署 URL
- 访问 `https://gameschartweb.你的用户名.workers.dev`
- 测试应用功能

## 🔍 预期的构建日志

成功的部署应该显示：

```
✅ Cloning repository...
✅ Installing dependencies...
✅ Running build command: npm run build
✅ Build completed successfully
✅ Running deploy command: npx wrangler deploy
✅ Successfully published your script
```

## ❌ 如果遇到错误

### 错误 1: 找不到 workers-site/index.js
**原因**: 文件未提交到 Git

**解决**:
```bash
git add workers-site/
git commit -m "Add workers-site files"
git push
```

### 错误 2: Module not found
**原因**: 依赖未安装

**解决**:
确保 `package.json` 包含：
```json
"devDependencies": {
  "@cloudflare/kv-asset-handler": "^0.3.0"
}
```

然后重新部署。

### 错误 3: package.json not found
**原因**: 根目录配置错误

**解决**:
检查 Cloudflare 控制台的根目录设置。

## 📝 提交前最后检查

运行以下命令确认文件都在：

```bash
# 检查 wrangler.toml
cat wrangler.toml

# 检查 workers-site/index.js
cat workers-site/index.js

# 检查 workers-site/package.json
cat workers-site/package.json

# 查看 Git 状态
git status

# 查看将要提交的文件
git diff --cached --name-only
```

## 🎯 推送命令

```bash
git add wrangler.toml workers-site/ package.json .gitignore
git commit -m "Configure Cloudflare Workers deployment"
git push origin main
```

## 🌐 部署后测试

访问你的应用并测试：

- [ ] 主页加载正常
- [ ] 搜索功能工作
- [ ] API 调用成功（检查浏览器 Network 标签）
- [ ] 图表显示正常

## 📞 获取帮助

如果部署失败：

1. 复制完整的错误日志
2. 检查 Cloudflare Dashboard 中的构建日志
3. 参考 `CLOUDFLARE_WORKERS_部署说明.md` 的故障排除部分

---

**准备好了吗？运行上面的 Git 命令开始部署！** 🚀

